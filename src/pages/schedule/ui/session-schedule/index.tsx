export const SessionSchedule = () => {
    // if (!schedule) schedule = SCHEDULE_NO_RESULT.schedule

    return null
    // return <Template events={schedule.session} />
}

export default SessionSchedule
