import { Size } from '@shared/ui/types'

export const OPEN_CLOSE_ANIMATION_DURATION = 200
export const DEFAULT_STATE_DURATION = 5000
export const HEIGHTS: Record<Size, number> = {
    small: 65,
    middle: 90,
    large: 95,
    big: 100,
}
